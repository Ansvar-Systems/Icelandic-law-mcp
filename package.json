{
  "name": "@ansvar/icelandic-law-mcp",
  "version": "1.0.1",
  "description": "Production-grade Icelandic legal research MCP server with comprehensive statute coverage and EU law cross-references",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "icelandic-law-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "data/database.db"
  ],
  "scripts": {
    "build": "tsc",
    "build:db": "node --import tsx scripts/build-db.ts",
    "build:db:paid": "node --import tsx scripts/build-db-paid.ts",
    "dev": "node --import tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "ingest": "node --import tsx scripts/ingest-althingi.ts --in-force-only",
    "ingest:all": "node --import tsx scripts/ingest-althingi.ts --include-repealed",
    "ingest:relevant": "node --import tsx scripts/ingest-althingi.ts --in-force-only --max-laws 400",
    "ingest:auto-all": "node --import tsx scripts/ingest-althingi.ts --in-force-only",
    "ingest:cases": "echo \"Icelandic case-law ingestion is not yet configured in this public build\"",
    "ingest:cases:full-archive": "echo \"Icelandic case-law ingestion is not yet configured in this public build\"",
    "sync:cases": "echo \"Icelandic case-law sync is not yet configured in this public build\"",
    "sync:cases:full": "echo \"Icelandic case-law sync is not yet configured in this public build\"",
    "sync:prep-works": "echo \"Preparatory works sync is not yet configured in this public build\"",
    "check-updates": "node --import tsx scripts/check-updates.ts",
    "extract:definitions": "node --import tsx scripts/extract-definitions.ts",
    "audit:seeds": "node --import tsx scripts/audit-seeds.ts",
    "fetch:eurlex": "node --import tsx scripts/fetch-eurlex-metadata.ts",
    "import:eurlex-documents": "node --import tsx scripts/import-eurlex-documents.ts",
    "migrate:eu-references": "node --import tsx scripts/migrate-eu-references.ts",
    "verify:eu-coverage": "node --import tsx scripts/verify-eu-coverage.ts",
    "test:contract": "vitest run __tests__/contract/",
    "drift:detect": "node --import tsx scripts/drift-detect.ts",
    "validate": "npm run lint && npm test && npm run test:contract",
    "lint": "eslint src tests",
    "prepublishOnly": "npm run build",
    "postinstall": "test -d dist || npm run build || true"
  },
  "dependencies": {
    "@ansvar/mcp-sqlite": "^1.0.3",
    "@modelcontextprotocol/sdk": "^1.25.3"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.15.29",
    "@vercel/node": "^5.6.4",
    "@vitest/coverage-v8": "^4.0.18",
    "fast-xml-parser": "^5.3.5",
    "jsdom": "^27.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ansvar-Systems/icelandic-law-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/Ansvar-Systems/icelandic-law-mcp/issues"
  },
  "homepage": "https://github.com/Ansvar-Systems/icelandic-law-mcp#readme",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai",
    "claude",
    "icelandic-law",
    "iceland",
    "legal",
    "compliance",
    "althingi",
    "lagasafn",
    "ansvar"
  ],
  "author": "Ansvar Systems AB <hello@ansvar.ai>",
  "license": "Apache-2.0",
  "mcpName": "eu.ansvar/icelandic-law-mcp"
}
